import 'package:flutter/material.dart';

class ConstantSettings {
  int id;
  String? title;
  String? leadingImage;
  Color? color;

  ConstantSettings(
      {required this.id, this.title, this.leadingImage, this.color});
}
